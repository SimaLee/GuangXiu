<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- 共享元素动画 style-->
    <style name="OptionsActivity" parent="AppTheme">

    </style>

    <!-- 首页的text背景 -->
    <style name="TextBackgroundStyle">
        <item name="android:textColor">@color/colorTextContent</item>
        <item name="android:textSize">25sp</item>
        <item name="android:textStyle">normal</item>>
        <item name="android:paddingLeft">10dp</item>
        <item name="android:paddingRight">10dp</item>
        <item name="android:paddingBottom">6dp</item>
        <item name="android:paddingTop">6dp</item>
        <item name="android:background">@drawable/selector_main_btn</item>
    </style>

    <style name="TitleTextStyle">
        <item name="android:textColor">@color/colorWhite</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">normal</item>
        <item name="android:paddingLeft">15dp</item>
        <item name="android:paddingRight">6dp</item>
        <item name="android:paddingBottom">3dp</item>
        <item name="android:paddingTop">3dp</item>
        <item name="android:background">@color/buttonClickColor</item>
    </style>

    <style name="popupWindowAnimRight">
        <item name="android:windowEnterAnimation">@anim/page_anima_right_in</item>
        <item name="android:windowExitAnimation">@anim/page_anima_right_out</item>
    </style>
    
    <style name="TabLayoutTextAppearance">
        <item name="android:textSize">18sp</item>
    </style>

</resources>
